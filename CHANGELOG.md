# 更新日志

记录 QuotaBar 应用的功能新增、样式调整和问题修复，按版本与日期倒序排列。
服务器迁移、官网、构建与发布脚本等不影响应用本身的改动不在此记录。

## 0.5.6 · 2026-09-16

### 2026-09-16

#### 样式

- 刘海岛总览页的「分享用量卡片」按钮移到面板右上角，只显示分享图标；花费条下方不再有按钮。
- 刘海岛总览页各工具的花费条改用和额度页一样的格子进度条；同时有三个工具时，格子条和间距略微压矮，面板高度不变。
- 刘海岛展开面板的额度卡片和用量页数字整体下移约 2pt，与服务商标题拉开一点，和底栏分隔线之间的空隙相应缩小，上下留白基本相等；面板高度不变。
- 各处按工具区分的用量和花费里，「Codex CLI」改称「Codex」，与 OpenAI 的正式名称一致。
- 设置 → 关于里的网站、GitHub、X、邮件四个链接去掉了名称和右上箭头，只保留图标和地址。
- Quota Run 设置页顶部说明里的「个人纪录」改为「个人记录」，与页面其他地方用词一致。
- 设置里所有单选切换控件统一宽度：每个选项 100pt，两个选项 200pt、三个 300pt，同一张卡片里的控件左右对齐；英文等较长的标签会按最长的一项自动加宽，不再被截断。
- 设置 → 更新里的「立即检查」改为和「立即刷新」一样的描边按钮；实心按钮只留给安装、保存、登录这类会改变状态的操作。
- 设置 → 关于里的开源致谢列表去掉右侧箭头，与上方链接一致。
- 「紧迫排行」桌面卡片的列表紧贴标题往下排，服务商较少时不再在标题下方空出一大块。
- 刘海岛展开面板只显示套餐本身的额度窗口，不再显示 GPT-5.3-Codex-Spark 这类单个模型的限额：Codex Plus 显示 5 小时和周两条进度条，Pro 只显示周这一条，与 codex-island 一致。只有单个模型限额的服务商照常显示。
- 刘海岛展开面板里只有一个额度窗口时（如 Codex Pro 的周额度），进度条占满整栏宽度，右端与两个窗口并排时对齐；圆环和数字样式的卡片改为靠左，与服务商标题对齐，不再居中。

#### 修复

- 中号「环形仪表」桌面卡片底部的账号栏超出卡片约 23pt 被裁掉；中号卡片的圆环和间距略微缩小，内容完整放进卡片。
- 「服务商网格」桌面卡片里，DeepSeek 这类余额格比旁边的额度格矮一截；现在余额数字与百分比同样大小，格子等高。
- 经典样式桌面卡片和边缘停靠条的圆环与百分比一直按「已用」显示，与旁边进度条和菜单栏的「剩余」口径相反；现在跟随外观设置里的「填充口径」。

### 2026-09-15

#### 新增

- 刘海岛和边缘停靠条收起时，只要有一项额度剩余不到 15%，外框就会一明一暗地闪烁提醒（参考 codex-island）：剩 15% 以内是琥珀色，剩 5% 以内变红，额度恢复后自动停止。按屏幕上显示的整数百分比判断，不受提醒阈值和通知开关影响；打开「减少动画」时外框常亮不闪。

#### 样式

- 刘海岛展开后不再在面板外面绘制光晕、流光和投影，窗口也不再比面板大一圈，截图时两侧和底部不会带出空白；收起时的光晕照旧。
- 刘海岛展开面板里「已同步 X 分钟前」原本顶部和底部各显示一次，现在只留在右下角，旁边新增立即刷新按钮，刷新时换成转圈并显示「正在刷新…」；底部的翻页圆点保持居中。
- 刘海岛展开面板改为固定高度：条形、阶梯、趋势线、数字、圆环五种样式，以及额度、用量、总览三页切换时面板高度都保持一致。高度按最高的内容定，比原来矮了约 26pt，进度条下方不再留一大段黑色空白；较矮的样式在这个高度里垂直居中，圆环因此与服务商标题拉开距离。总览页的花费条和分享按钮间距略微收紧，三个工具时也放得下。
- 刘海岛趋势线样式去掉了「趋势 · 最近 N 次刷新」说明文字，避免和重置时间挤在一起；折线改为跟随「已用 / 剩余」切换，和百分比、进度条的方向一致。
- 刘海岛展开面板左上角的 QuotaBar 字标前加上应用 logo，用和文字相同的白色。

#### 修复

- 退出应用时立即保存 Quota Run 本地成绩记录。此前记录改动后要等 5 秒才写入磁盘，在这 5 秒内退出会丢失这次改动。

## 0.5.5 · 2026-09-15

### 2026-09-15

#### 样式

- 分享用量卡片右下角的网址改为 Quota Run：登录了 Quota Run 并打开「署名」时显示你的主页 quota.run/@用户名；没有登录时只显示 quota.run，署名写在网址前面，不再拼成 quota.bar/名字 这样并不存在的地址。分享窗口里登录后署名框换成说明，写明会显示哪个主页。

## 0.5.4 · 2026-09-15

### 2026-09-15

#### 新增

- 右键「复制为图片」时，服务商卡片上的登录账号默认显示为马赛克，图片里不再出现邮箱地址；马赛克是固定图案，不透露账号内容和长度。可在设置 → 通用 → 隐私里关闭「复制图片时遮挡账号」，下拉面板里的账号照常显示。
- 服务商卡片的右键菜单新增「隐藏额度」，可以把用不到的额度彻底隐藏，例如 Codex 的 GPT-5.3-Codex-Spark：隐藏后卡片上和展开箭头下都不再显示，圆环、刘海岛、桌面卡片和菜单栏也不再跟随它，快用完的提醒同样不再发。每个服务商分别记住，切换界面语言后照样有效，点「全部显示」恢复；最后一项不能隐藏。停靠条圆环的右键菜单里也有这一项。本机接口 `/v1/limits` 仍返回全部额度。（#2）
- Codex 卡片的「限额重置次数」下面新增「到期」一行，列出每次攒下的重置机会各自什么时候过期，最早的在前，例如「5 天 18 小时 · 18 天 23 小时 · 19 天 22 小时」；超过三次时显示其余次数。和额度行一样可以点击在倒计时和具体时间之间切换，悬停显示另一种写法。没有过期时间的不列出，停靠条圆环的详情里也有这一行。（#3）
- Codex 送的限额重置读得更全：「限额重置次数」旁显示累计获得了多少次，可用次数用完了、以前领到过也照样显示这一行；悬停列出每一次的名称（例如「Full reset (Weekly + 5 hr)」）和到期时间。收到新的重置机会时推送通知，写明现在共几次、最早哪天到期；某一次还没用、离到期不到一天时再提醒一次，每次只提醒一次。可在设置 → 提醒 → 额度重置里关闭「赠送的限额重置」。明细最多一小时读一次，次数变化或有一次到期时立刻重读。
- 充值型服务商不再显示空进度条：DeepSeek、Kimi 开放平台、OpenRouter、小米 MiMo 的卡片改为大字显示各币种余额（有赠送时分列充值和赠送，余额不足以调用 API 时标红）。下面是最近用量，可在「今日」「7 天」「30 天」「全部」之间切换，显示花费并画成柱状图或折线图：今日按小时、7 天和 30 天按天、全部按月，右上角切换图表样式并记住，悬停某一柱看这一小时、这一天或这个月的明细。服务商提供明细时（例如 OpenRouter 填管理密钥），图表下还可以按 API Key 或按模型查看，默认列出用得最多的 3 个，点击某个 Key 看它的详情。停靠条悬停弹出的详情卡同样显示余额区。桌面卡片「大数字」「环形仪表」选中充值型服务商时改为余额卡片（大字余额，今日、7 天、30 天消费，大尺寸再画出 30 天用量图），「服务商网格」「紧迫排行」里显示余额金额、不画进度条。刘海岛和菜单栏右键菜单里显示余额金额，不再是「—」。复制为图片时显示 30 天的消费和用量图，不含 Key 名称。小米 MiMo 的 Token Plan 月额度仍按进度条显示。
- 只填 API Key 就能看到最近用量：DeepSeek、Kimi 开放平台、小米 MiMo 的接口只返回余额，QuotaBar 会记下每次读到的余额，按余额的减少估算今日、7 天、30 天和全部的消费并画图，卡片上标明「约消费」和开始记录的时间；余额上涨视为充值，不计入。
- 余额提醒：设置 → 提醒 →「花费」新增「余额低于」，按设置时的货币填一个金额（留空即不提醒），DeepSeek、Kimi 开放平台、OpenRouter、小米 MiMo 的账户余额低于它、或已不足以调用 API 时推送一次通知；多个币种的余额按汇率合计后比较，充值后再次跌破会重新提醒。
- OpenRouter 填管理密钥（Provisioning Key）时列出账户下所有 Key 的花费；填普通 API Key 时显示余额和这个 Key 自己的花费。OpenRouter 按它自己的自然日、自然周、自然月统计，所以「7 天」「30 天」对应它的本周、本月。

#### 修复

- 阿里云百炼一直提示「会话已过期」，其实并没有过期：百炼控制台页面改了写法，QuotaBar 取不到请求需要的 sec_token；凭据框里填成 API Key 时也被报成会话过期，回复里只要出现 login 字样也会误判。现在能从新页面取到 sec_token，并改用控制台当前的接口名；账号没有订阅 Coding Plan 时直接说明（阿里云已改卖 Token Plan，暂时读不了它的额度）；填的是 API Key 时提示改用「浏览器登录…」或粘贴 Cookie，Qwen Cloud、小米 MiMo、Qoder 同样。国内站和国际站都试过后，报出最能说明问题的那个原因。
- DeepSeek 填好 API Key 后一直提示「服务商响应无法解析」、读不到余额：现在能正确显示账户余额，有赠送余额时分列充值和赠送；账户同时有人民币和美元余额时各显示一行，余额不足以调用 API 时会注明。（#1）
- Grok 卡片上「每周额度」和「Grok Build」两条一模一样：本周额度只用在一个产品上时，那个产品的额度条和总额度数字、重置时间完全相同，现在不再重复显示；用在多个产品上时照常分项列出。（#2）
- 设置里的输入框按 ⌘V 粘贴没有反应，粘贴 API Key 时看起来像输入框不接受输入：现在 ⌘V 粘贴、⌘C 拷贝、⌘X 剪切、⌘A 全选、⌘Z 撤销在设置和其他窗口里都能用，⌘W 关闭窗口。

### 2026-09-14

#### 新增

- 设置新增「项目」页：从本机 Claude Code、Codex、OpenCode 的会话日志里认出每次用量属于哪个项目。按 Git 仓库的远端地址识别，同一个仓库的不同目录和 worktree 算一个项目，没有仓库的按文件夹分；同名文件夹会并进对应的仓库。可以按 7、30、90、365 天查看概览（项目数、等价花费、token、会话数、编程方式数），以及按花费排序的项目排行（按工具分色的占比条）。点开一个项目，能看到每天的花费柱状图（悬停看当天数字）、按工具和编程方式（命令行、桌面版、编辑器插件、SDK）的占比、用得最多的模型，以及按星期和小时的活跃热力图。文件夹路径只留在本机。
- 项目公开到 Quota Run：登录 Quota Run 后，每个项目有「公开到 quota.run」开关，默认全部不公开，还可以改公开名称，公开后可以一键打开项目页。这台 Mac 会按天上传各项目、各工具、各编程方式、各模型的 token 数：公开的项目带名称和仓库地址，不公开的合并成「其他项目」、不带名字；路径和会话内容永远不上传。花费由 quota.run 按公开价目统一计算。改动开关或名称后会把全部日子重传一遍，关掉的项目立刻从网站上撤下。上传同意说明里也补上了这一项。

### 2026-09-13

#### 新增

- 菜单栏图标的右键菜单和下拉面板的「更多」菜单新增 Quota Run 入口：未登录时显示「登录 Quota Run…」，点击打开设置并直接滚动到登录卡片；已登录时显示「Quota Run · @用户名…」，点击打开 Quota Run 设置页。
- 下拉面板服务商卡片的右键菜单新增三组快捷设置：「卡片上显示的额度」逐项勾选哪些额度显示在外面、哪些收进展开箭头（每个服务商分别记住，可恢复默认，最后一项不能取消）；「圆环跟随的额度」选择停靠条圆环、刘海岛和菜单栏跟随的窗口，与双击窗口是同一个设置；「显示位置」勾选这个服务商是否出现在下拉面板、停靠条、刘海岛和桌面卡片，另有「菜单栏只显示此服务商」和「添加一张此服务商的桌面卡片」。原来的「在下拉面板中隐藏」和「圆环改回按最满的窗口显示」并入这几组。
- 在线更新改为更新卡片：发现新版本时弹出一张卡片，显示新版本号、当前版本、发布日期和这一版的更新内容（按界面语言显示中文或英文，分新增、样式、修复，可展开全部并打开完整更新日志），点「安装并重启」后下载、验证开发者签名和 Apple 公证，完成后自动替换并重启；不会再不打招呼就重启。「更新方式」改为「后台下载」（发现后先在后台下载验证好，点安装即刻完成）和「安装时再下载」。原地替换失败（例如没有「应用程序」文件夹的写入权限）时，卡片说明原因并提供「重试」和「下载安装包」。菜单栏右键菜单、面板顶部横幅和设置里的更新页都打开这张卡片；同一版本每次启动只自动弹出一次。
- 花费预算提醒：设置 → 提醒新增「花费」卡片，可设每日预算和每月预算（按设置时选的货币，留空即不设），花费用到 80% 和超出时各发一次通知，按天和按自然月分别计算。
- 每周用量周报：每周一上午 9 点后推送一条通知，写明上周的花费、token 数和用得最多的 CLI 所占比例；上周没有用量时不发。可在设置 → 提醒 →「花费」里关闭。
- 额度重置日历：设置 → 用量统计顶部新增「接下来 7 天的额度重置」，按天列出已开启服务商的每个额度窗口什么时候重置（今天、明天、具体日期和时间），以及当前剩余或已用，快用完的标成橙色或红色。
- 停靠条圆环的右键菜单也加上「卡片上显示的额度」「圆环跟随的额度」「显示位置」三组，和下拉面板卡片的右键菜单一致。
- Quota Run 额度竞速：设置里新增「Quota Run」页。不注册也能用：每次读数自动记进本机成绩账本（保留 60 天），显示进行中的额度窗口和个人最佳（最快用满、用到 50%/90% 的时间、最高峰值），成绩可以复制或保存为分享图片。想上榜时，看过并勾选上传清单后点「用 quota.run 登录」：应用打开浏览器并显示一个验证码，在 quota.run 用 Google、GitHub 或邮箱验证码登录（第一次登录时选用户名），核对验证码后批准连接这台 Mac，应用不经手任何密码或第三方令牌；之后这台 Mac 用存在安全芯片里的签名密钥，作为计分设备把额度读数和每分钟 token 数签名上传到 quota.run，成绩和排名由服务器计算，排行榜在 quota.run，个人主页在 quota.run/@用户名。登录后页面显示 @用户名和登录方式、每台 Mac 的 QuotaBar 版本，可编辑资料和最多 12 个项目，「在 quota.run 管理账号」打开网页账号页；另一台 Mac 用同一个账号登录即可加入，更换计分设备有 7 天冷却；「断开这台 Mac」保留账号和本机记录，「删除账号」删除 quota.run 上的全部数据，两者都要确认。上榜成绩、每一轮的用量曲线和主页上每天 token 用量的热力图在 quota.run 上公开（热力图可以在网页账号页关掉），登录邮箱只用于登录，不会显示在主页或榜单上，上传同意说明里也写明了这一点。凭据、令牌、提示词、代码、文件路径和明文邮箱永远不上传；不加入就什么都不上传。关于页的联网清单相应补上 Quota Run。
- Quota Run 服务商账号绑定：只有能确定来自哪个服务商账号的读数才上传和上榜，应用只上传账号邮箱的单向摘要，邮箱本身不离开这台 Mac；一个 Codex、Claude 等服务商账号只归一个 Quota Run 账号，先上传的先得，登录邮箱与该服务商账号邮箱一致的可以认领，成绩上显示「账号已核实」。设置 → Quota Run 新增「服务商账号」卡片，按服务商列出这台 Mac 登录的账号（邮箱打码显示）和状态：尚未上传、已绑定、账号已核实、被其他 Quota Run 账号占用（附认领方法）；可「解绑」（确认后删除这个账号在 quota.run 上的读数和成绩，并停止从这台 Mac 上传），也可「重新绑定」。个人最佳里没有绑定账号的一轮标为「未绑定服务商账号，不会上榜」。

#### 样式

- 设置里的说明文字不再默认显示在每一项下面：原来挤在标题下方、常被拆成两行的说明，改为标题旁的小问号，鼠标悬停（或点击）时弹出说明；整组的补充说明放在卡片标题旁的问号里。代理地址填错时的提示仍直接显示在输入框旁，简介和项目描述的字数改在输入框旁显示，「选填」写进输入框的占位文字。
- 设置 → 服务商里展开一行时，「测试连接」和「控制台」不再单独占一行：「控制台」紧跟在服务商名字后面，「测试连接」放在右侧服务状态之前，与名字水平对齐；测试结果仍显示在展开内容下方。需要保存凭据、浏览器登录或授权钥匙串的服务商，这些按钮仍在下方一行。
- 设置窗口右侧每个分区的标题和说明文字改为同一行显示，按文字基线对齐，不再上下两行；窗口太窄时说明文字先截断，鼠标悬停可看全文。
- 服务商行标题里的「测试连接」改为小胶囊按钮（带图标，和状态标签差不多高），不再是 30pt 高的大按钮，展开的行不会比收起时高出一截；展开内容里「服务状态」一行的文字与左侧标签对齐，之前比标签高出约 7pt。
- 服务商卡片默认显示的额度按套餐调整：Codex 只显示套餐本身的额度，有 5 小时限制的套餐显示 5 小时和周窗口，没有 5 小时限制的 Pro 套餐只显示周窗口，GPT-5.3-Codex-Spark 的额度收进展开箭头；Claude 显示 5 小时、周窗口和 Fable 三项。圆环正在跟随的窗口始终显示在外面。其他服务商不变。
- 下拉面板里拖动服务商卡片时，拖到列表顶部或底部附近会自动滚动，越靠边滚得越快，卡片一直跟着鼠标，服务商很多时也能拖到最上面或最下面。

#### 修复

- 切换界面语言后，「圆环跟随的额度」和「卡片上显示的额度」这两项选择会失效：额度窗口的名字随语言变化（「周窗口」和「Weekly window」），选择是按名字记住的。现在切换语言重新读取时，会把选择对应到同一个窗口的新名字上。
- 本地接口（127.0.0.1:6736）只回应发给 127.0.0.1 或 localhost 的请求，并拒绝来自其他网站的跨站请求，防止网页通过 DNS 重绑定读到用量数字；其他请求返回 403。
- 在 Codex、Claude 等 CLI 里切换到另一个账号后，下一次读取可能被误认为「额度重置」（新账号用量更低、重置时间不同），弹出重置横幅和通知，趋势图也会把两个账号的数据接成一条线。现在读到的账号和上次不同时，不算重置，这个服务商的趋势重新开始记录。

## 0.5.3 · 2026-09-13

### 2026-09-13

#### 新增

- 菜单栏图标的右键菜单重做：新增「显示在菜单栏」，可选「自动（用得最满的额度）」或任一已开启的服务商，每项带 logo 和图标会显示的读数（剩余或已用），当前选择打勾；这个选择只影响菜单栏，停靠条、刘海岛、桌面卡片仍按各自的设置显示。菜单还新增「检查更新…」（有新版本时显示「更新到 x.y.z…」，下载好后显示「重新启动以更新到 x.y.z」）、「反馈…」和「关于 QuotaBar」，保留立即刷新（⌘R）、设置（⌘,）和退出（⌘Q）。

#### 修复

- 关闭了自动检查更新时，更新页的「立即检查」和面板选项菜单里的「检查更新…」点了没有反应：手动检查现在不受自动检查开关限制。

## 0.5.2 · 2026-09-13

### 2026-09-13

#### 新增

- 边缘停靠条在服务商太多、屏幕放不下时可以滚动：条子最高到屏幕上下各留 24pt，其余圆环用滚轮或触控板在条子里滚动。系统滚动条换成内侧一条 2pt 的细线，静止时很淡，滚动时稍亮，停下后淡回去；上下还有更多圆环时，两端渐隐到黑色。滚动时悬停卡片先收起，悬停卡片与圆环的对齐会算上滚动距离；额度重置时，对应的圆环会先自动滚到可见处。
- 下拉面板里的服务商卡片可以按住拖动排序：拖动时卡片微微浮起、跟着鼠标走，经过相邻卡片一半时两张互换位置，其他卡片滑开让位；松手后卡片落位，新顺序同时用于停靠条、刘海岛和桌面卡片。卡片里的点击（切换已用和剩余、重置时间等）不受影响，移动超过几个点才开始拖动；在面板外松手也会正常落位并保存。

#### 样式

- 关于页的 GitHub 链接显示为仓库的新名字 gentpan/QuotaBar，反馈、更新检查等地址也一并换成新地址。

## 0.5.1 · 2026-09-13

### 2026-09-13

#### 新增

- 服务商可以按位置隐藏，而不必停用：下拉面板、停靠条、刘海岛、桌面卡片各自决定显示哪些服务商。隐藏的服务商仍会读取数据、发提醒、计入花费，停用才会停止读取。设置 → 展示方式新增「各处显示的服务商」，每个服务商一行，点位置标签即可显示或隐藏；也可以右键下拉面板里的卡片选「在下拉面板中隐藏」，右键停靠条图标选「在停靠条中隐藏」。
- 下拉面板底部在有隐藏的服务商时显示「N 个已在面板隐藏」和它们的图标，点「显示」可逐个恢复或打开设置管理；停靠条右键菜单也列出已隐藏的服务商。
- 应用内更新在连不上 GitHub 时改从 quota.bar 服务器检查和下载，打不开 GitHub 的网络也能收到更新；下载到的安装包照旧要验证开发者签名和 Apple 公证才会安装。关于页「你的数据」里的联网说明相应补上这一项。

#### 样式

- 下拉面板页脚的刷新提示：点「全部刷新」或自动刷新完成后的一分钟内显示「刚刚更新」，之后显示「X 分钟后自动刷新」；鼠标悬停显示上次更新和下次自动刷新的具体时间及刷新间隔。以前刷新完立刻显示「5 分钟后刷新」，看起来像按钮没起作用。
- 节奏提示的文案改为「预计重置时剩 8%」，不再写成含糊的「约 8% 余量」；鼠标悬停时写出算法和用到的数字，例如「这个窗口已过 87%，已用 80%。按这段时间的平均速度，到重置时约用到 92%，剩 8%」。
- 服务状态的「运行正常」改为「服务正常」；悬停状态点或状态标签时写明来源，例如「来自官方状态页 status.claude.com，按 Claude Code、Claude API 判断，3 分钟前检查」，说明它反映的是服务商官方的服务状态，不是本机登录或额度读数。
- 边缘停靠条靠近时只展开出图标，不再立刻弹出左侧卡片：等停靠条完全展开后，把鼠标移到某个图标上才显示该服务商的详情卡片；之后在图标之间移动，卡片会直接跟着切换。锁定显示时悬停图标直接显示卡片。
- 边缘停靠条的展开改为两段：小胶囊先横向拉宽，再纵向拉高，圆环在形状里淡入滑出，不会露在黑色区域外；收起时先缩短高度，再收窄回胶囊。
- 字标 QuotaBar 的字体从 Sora 换成 Instrument Sans 半粗，设置侧边栏、关于页、下拉面板、刘海岛、分享卡片和复制图片的底栏都跟着换；关于页开源致谢里的字体一并改为 Instrument Sans。
- 分享用量卡片窗口不再有单独的灰色标题栏：标题栏改为透明，卡片预览和右侧设置一直铺到窗口顶部，和设置窗口一样是一整块；卡片和右侧标题避开左上角的红绿灯按钮并上下对齐。

#### 修复

- 英文界面的设置 → 服务商列表里，「Keychain」标签被挤成两行（Keychai / n）：状态标签的位置加宽，文字固定一行。
- 额度窗口刚开始时节奏估算会乱报（例如 5 小时窗口开始 3 分钟用了 3%，就推算成会用完）：现在至少过了窗口时长的 5%、且不少于 15 分钟，才给出余量、会用完和用完时间的判断；已经用完的仍会立即显示。
- 边缘停靠条悬停展开时会先瞬间离开屏幕边缘、露出一条缝隙：窗口不再在展开和收起时改变尺寸，只让黑色形状在窗口里生长，全程贴着屏幕边缘；没画出来的透明区域让鼠标直接穿透。

## 0.5.0 · 2026-09-13

### 2026-09-13

#### 新增

- 下拉面板回归：左键点菜单栏图标打开，右键仍是刷新、设置、退出。面板顶部是花费卡片，下面每个服务商一张卡片，底部显示版本、下次刷新倒计时、设置和选项菜单。按 Esc 关闭，⌘R 刷新，⌘, 打开设置。
- 花费卡片：可切换花费、Token、每百万 token 花费三种口径，以及今日、昨日、近 30 天。环形图按来源分色，中间数字滚动变化，悬停来源查看按模型的明细。
- 服务商卡片默认只显示最重要的两个额度窗口，其余窗口、限额重置次数、30 天用量趋势、今日昨日 30 天花费、状态页和控制台链接收在展开箭头里，展开状态会被记住。
- 进度条加入节奏判断：细竖线标出匀速使用时此刻应在的位置。预计余量很紧时显示约多少余量，预计重置前用完时显示火苗和用完时间，已经用完显示已到上限。悬停进度条查看按当前速度重置时的用量。
- 点击百分比在已用和剩余之间切换，点击重置时间在倒计时和具体时刻之间切换，所有界面同步生效。
- 右键服务商卡片或花费卡片可复制为图片，按 4 倍分辨率放进剪贴板，并弹出已复制提示。
- 周用量分享卡：按 API 价值或 token 数展示最近 7 天、30 天、3 个月、今年或全部时间，金额满 1000 美元变黑卡，满 1 万美元变蓝卡。可选 4:5、1:1、9:16 比例和署名，支持系统分享、保存 1080 像素 PNG、复制图片和文案。
- 用量存档：QuotaBar 自己保存每天每个模型的 token 与花费，Claude Code 清理旧日志后统计也不会变少。
- 启动时先显示上次保存的读数，再在后台刷新。
- 首次安装时自动检测本机已登录的工具，只开启对应服务商，并显示一张可关闭的欢迎卡片。
- 共享屏幕或录屏时可隐藏用量：菜单栏只显示 logo，停靠条、刘海岛和桌面卡片暂时隐藏。
- 三种节奏通知：快用完、余量很紧、重置前会用完，同一周期只提醒一次。
- 花费可显示为人民币、港币、日元、欧元等货币，汇率每天更新一次。
- token 统计口径可选包含缓存或仅输入和输出。
- 服务商请求可走 HTTP 或 SOCKS5 代理。
- 刘海岛光晕：黑色轮廓外一圈钴蓝柔光，接近告警线时渐变成琥珀色或红色，一道光沿轮廓环绕。可开启低功耗模式，只在刷新、悬停或告警时发光；刘海岛被全屏应用遮住时自动暂停动画。光晕区域不挡住鼠标点击。
- 刘海岛越线自动弹出：额度第一次超过告警线时，刘海岛自动展开约 4 秒再收回。
- 刘海岛面板分为额度、用量、总览三页，双指左右滑动或点底部圆点翻页。总览页显示今日和近 30 天花费、各来源占比，并可打开分享卡片。
- 刘海岛额度图表有条形、圆环、阶梯、数字、趋势线五种样式，点底部标签或在面板上按住 ⌘ 点击切换。
- 同步状态点改为缓慢呼吸，每次拿到新数据时轻跳一下。
- 桌面卡片：详细模式的进度条加上节奏刻度和重置倒计时，底部显示今日和近 30 天花费；标准模式在圆环下显示重置倒计时；可按紧迫度排序。
- 全局快捷键：在设置里录制一个组合键，在任何地方打开下拉面板。
- 本地接口：开启后本机其他工具可以从 http://127.0.0.1:6736/v1/limits 读取额度，不含凭据和账号；终端运行 `QuotaBar --json` 输出同样的数据。
- 测试版更新：开启后同时接收预发布版本。
- 设置页新增：变色方式、重置时间格式、12 或 24 小时制、始终显示节奏、减少动画、面板密度、显示花费卡片、刘海岛光晕与低功耗、越线自动弹出、刘海岛图表样式、桌面卡片按紧迫度排序、三种节奏提醒、货币、token 统计口径、共享屏幕时隐藏用量、代理、本地接口。
- 用量统计页和下拉面板都能打开用量分享卡片，版本更新后如果本周有用量会自动打开一次。
- 菜单栏新增"logo 加数字"模式：选中了服务商时只显示它，否则显示前三个已启用服务商的 logo 和百分比。
- 右键服务商卡片可以上移、下移，停靠条、刘海岛、桌面卡片和面板按同一顺序排列。
- 下拉面板可设为半透明，让桌面透过面板显示。
- 新增 10 个服务商，共 23 个：阿里云百炼 Coding Plan、火山方舟、智谱 GLM（国内站）、Kimi 开放平台余额、GitHub Copilot、OpenRouter、小米 MiMo、Qoder、Windsurf、Kiro。Copilot 直接使用本机 GitHub CLI 的登录；火山方舟读 arkcli，Windsurf、Kiro 读本机已登录的客户端；阿里云百炼和小米 MiMo 支持在应用内浏览器登录。除 Copilot 外暂时标为"实验性"，等真实账号验证。Copilot、Windsurf、Kimi 开放平台接入了公开状态页。
- 新增诊断命令 `QuotaBar --provider <服务商>`，单独拉取一个服务商的读数。
- 桌面卡片支持多张：大数字、环形仪表、花费趋势、每日对比、服务商网格、紧迫排行六种新样式，加上原来的经典样式；每张可选小、中、大三种尺寸，可指定服务商或统计来源，位置分别记住。右键卡片可更换样式、尺寸、服务商、添加卡片或删除，双击打开下拉面板。设置里的桌面小工具改为卡片列表，可添加卡片或恢复默认两张。原来开着桌面小工具的，会在原位置换成默认的两张：主服务商大数字，下面是花费趋势。
- 面板页脚新增"全部刷新"按钮：重新读取所有服务商、服务状态和本地日志，并重新读取登录凭据，刷新期间转圈。⌘R 和选项菜单里的刷新也改为全部刷新。单张卡片右上角的按钮仍只刷新该服务商。
- 关于页重做：GitHub 和 X 链接换上各自的品牌图标，新增邮件链接 hello@quota.bar；加入一段程序介绍；底部显示更新日期、更新日志入口、系统要求（macOS 14 或更高）、版权 © 2026 QuotaBar 和 MIT 开源许可；"你的数据"里列出 App 会连接的全部地址；新增"开源致谢"，列出 codex-island、OpenUsage、CodexBar、theSVG 和 Sora 字体的作者与许可；页尾加上与各服务商及 GitHub、X 无隶属关系的商标声明；标语改为"每个 AI 编码额度，抬眼就看见"，与官网一致。
- 额度重置时刻：5 小时、每周等额度窗口重置后，QuotaBar 会在重置时间点自动重新读取该服务商，不用等下一次定时刷新（服务商还没翻篇时每分钟再试，最多 3 次）。颜色一律跟随服务商（Claude 橙、Codex 蓝），菜单栏保持黑白。
- 额度重置 · 菜单栏图标：进度平滑回满，按钮底下亮一下，再有一道高光从左往右扫过图标。
- 额度重置 · 边缘停靠条：自动滑出，对应服务商的圆环被品牌色的光弧扫满一圈并向外扩散两圈波纹，圆环轻轻放大；旁边滑出「刚刚重置」卡片，可用百分比滚动到新值、进度条填满，并写明之前只剩多少；约 3 秒后收回，鼠标停在上面则保持打开。
- 额度重置 · 刘海岛：像灵动岛一样带回弹展开一行，迷你圆环画满，显示「额度已重置」、服务商和窗口名，数字滚动，光晕用服务商的颜色，约 4 秒后收回。
- 额度重置 · 下拉面板和卡片：对应额度行先用品牌色亮一下再淡回，「刚刚重置」标签弹出、箭头转一圈，保留十分钟；桌面卡片右上角的状态也暂时换成「刚刚重置」。
- 重置通知：可选关闭、用满后（默认，只在该窗口用过 90% 以上时）或每次。应用关闭期间发生的旧重置不会补发提示。设置 → 提醒新增「额度重置」卡片；终端运行 `QuotaBar --simulate-reset claude` 可以预览效果。

#### 样式

- 停靠条悬浮卡片的额度行换成新的共用额度行，带节奏提示和点击切换。
- 展示方式里的"菜单栏"改名为"仅菜单栏"，分享卡片和具体重置时间的日期跟随界面语言显示。
- 面板页脚改为一行：版本号后面接下次自动刷新的时间。
- 复制为图片的底栏改为：左侧是绿色底的 QuotaBar 应用图标加名称，右侧单独放网址 quota.bar，不再写成"QuotaBar · quota.bar"。
- 花费卡片头部重构：左侧改为下拉标题，点开在花费、Token、每百万 token 花费之间选择；右侧说明、分享、复制改为三个同样大小、对齐的按钮，点说明会显示数据来源。三个图标缩放到同一个方框内，高度一致，并与左侧下拉标题的中心线对齐。
- 按钮按下时轻微缩小，动画曲线统一；图表切换时带轻微模糊过渡。支持"减少动画"，并跟随系统的减弱动态效果设置。
- 设置里桌面卡片的尺寸切换在英文界面下改为 S / M / L，不再把 Medium 截成 Med...；桌面多服务商卡片英文底栏写作"4 providers · % left"，大数字卡片的窗口名和"left"之间补上空格；"屏幕"说明的英文措辞重写。
- 设置窗口的控件统一为一套样式：下拉选择、分段切换、按钮、输入框同为 30pt 高，同样的圆角、底色和细边框，放在一行里高度对齐。桌面卡片的样式与服务商、货币选择改为新的下拉控件，点开时当前项叠在控件上并打勾，文字与控件对齐；"添加卡片"改为同款带箭头的按钮。
- 设置里的开关打开时显示为绿色，不再是深灰。只有一行标题的开关行与输入行等高，卡片内的行距一致；左侧标签与右侧控件垂直居中。
- 分享用量卡片窗口的下拉、署名输入框、开关和分享、保存、复制按钮换成同一套控件样式。

#### 修复

- 卡片翻到用量背面、刘海岛用量页、用量统计页在每次启动后第一次打开要等十来秒：现在直接从本地存档算出，打开即显示。
- 启动后花费卡片要等约 8 秒才有数字：现在启动时从本地存档读出，约 2 毫秒；后台只补读最近两天改动过的日志，之后每次约 30 毫秒。
- 启动后很快翻到卡片背面，会误显示"本地还没有记录"。
- 长时间运行时内存持续增长：正在写入的会话日志每次刷新都会多缓存一份，现在每个文件只保留一份，不再扫描到的文件会被释放。
- 停靠条在圆环之间切换卡片时不再每次重新计算卡片尺寸，切换更顺。
- Codex 的套餐标签分不清两种 Pro：现在按接口返回的套餐标识显示为 PRO 5X 或 PRO 20X。
- 刷新间隔最低改为 5 分钟，避免 Anthropic 用量接口限流；旧配置里的 1 分钟、2 分钟会自动改为 5 分钟。
- 切换界面语言后，额度窗口名称、套餐说明、错误信息仍停留在原来的语言，要等下次自动刷新才变：现在切换语言会立刻重新读取所有服务商和服务状态。
- 上次保存的读数会记录它是用哪种语言取得的，换了语言启动时不再显示另一种语言的旧文字（升级后首次启动会先读取一次，不沿用旧缓存）。
- 中文界面的服务状态不再显示状态页的英文标题（如 All Systems Operational），改为中文状态级别；具体事件名称仍保留状态页原文。
- 英文界面花费说明里的来源列表用了中文顿号，现在按语言使用逗号或顿号。
- 桌面卡片在还没有读数时，仪表环、服务商网格和排行进度条会画成满格绿色，节奏格显示"够用"：现在画成空的，数字和节奏显示横线。
- 关于页的联网说明漏写了一项：模型价目表也是从 GitHub 获取的（LiteLLM 价目表），现在已补上。

### 2026-09-12

#### 新增

- 悬浮卡片里双击某个额度窗口（5 小时、周窗口，或 Fable 这类按模型的额度），圆环、刘海岛、桌面卡片和菜单栏读数都改按这个窗口显示。再双击一次恢复为用得最满的那个。正在跟随的窗口名旁有圆点，手动选中时为品牌色实心点。
- 展示方式新增「屏幕」选项：接了多块显示器时，可以选择停靠条、刘海岛和桌面卡片显示在哪块屏幕，默认自动。所选屏幕拔掉后自动回到默认，插拔显示器时各窗口会重新定位。
- 服务状态页的每一行在折叠时就显示主服务最近 30 天的在线率和状态条。Claude 取 Claude Code，Codex 取 CLI，Cursor 取 IDE，Kimi 取 Open API，DeepSeek 取 API 服务。
- 服务状态标记改为只看写代码相关的服务：Claude 看 Claude Code 和 Claude API，Codex 看 CLI、VS Code 插件、Codex API、Codex Web 和桌面端，Cursor 看 IDE、CLI 和云端 Agent。状态页其他组件的事件（如 Claude Cowork）不再让标记变黄，展开服务状态行时单独列在「其他组件」下。
- 新增诊断命令 `QuotaBar --status`，列出各服务商状态标记依据的组件和其他组件的事件。
- 新增诊断命令 `QuotaBar --credentials`，显示各本机凭据能否读到以及读取方式，不输出密钥。

#### 样式

- 设置页不再闪出右侧滚动条，展开服务状态行时也不会。

#### 修复

- Codex 的状态读不到 CLI 组件：OpenAI 状态页的汇总接口不含 CLI，现在缺少时会补读完整组件列表。
- 重装或更新后，打开应用又弹出「Claude Code-credentials」钥匙串授权对话框。现在改为通过系统自带的 security 工具读取，与 Claude Code 自己的读取方式相同，不再因应用签名变化而重复询问。

## 0.4.0 · 2026-09-12

#### 新增

- 服务状态：读取 Claude、OpenAI（Codex）、Cursor、Manus、MiniMax、Kimi、DeepSeek、Gemini 的公开状态页，每五分钟一次，无需登录。故障会显示在悬浮卡片和设置里。
- 设置新增「服务状态」页：每个服务商一行，点开可看各组件状态和最近 30 天的在线情况。
- 设置新增「用量统计」页：本机 Claude Code、Codex CLI、OpenCode 会话日志的全年热力图和 token 用量。
- 新服务商 Antigravity 和 Qwen Cloud。
- Cursor 增加 Grok Bot 额度；Grok 和 Claude 卡片显示登录账号。
- 刘海岛重做：刘海两侧各显示 1 到 3 个服务商，悬停时从顶部展开完整面板。面板底部可切换阶梯或连续进度条、已用或剩余、额度或用量。
- 停靠条单击圆环打开完整卡片，可把服务商「钉到」菜单栏、刘海岛、停靠条或桌面卡片。
- 停靠条右键菜单增加立即刷新、锁定显示、显示全部服务商。
- 悬浮卡片可以翻面查看用量：今日、7 天、14 天、30 天的用量与日均，柱形图悬停显示当天数值。
- 悬浮卡片的刷新按钮可单独刷新该服务商，刷新时旋转。
- 菜单栏图标可选显示读数、QuotaBar 白色 logo，或不显示。
- 点击菜单栏图标直接打开设置窗口，不再弹出下拉面板。
- 开启一个还没登录的服务商时，会自动展开并给出登录指引。Cursor 和 Kimi 支持在应用内浏览器登录。
- 更新可选自动下载安装并重启，或手动安装；更新页显示当前版本。
- 反馈页：无需登录，直接提交到 quota.bar。
- 关于页加入官网、GitHub 和 X 链接。
- 设置里的进度条默认改为阶梯样式，可切回连续样式。
- 桌面卡片可选显示全部服务商或只显示钉住的一个。

#### 样式

- 停靠条圆角改为 14pt，去掉直角选项。
- 停靠条圆环里的 logo 放大；去掉圆环下方的百分比，选中的服务商在圆环下方显示绿点。
- 停靠条向屏幕边缘偏移，抵消屏幕自带的黑边，视觉上居中。
- 停靠条圆环悬停时微微放大，卡片在圆环之间移动时平滑过渡。
- 刘海岛两侧的 logo 与数字使用同一种品牌色。
- Kimi 的 logo 去掉黑色底块；Qwen 和 Antigravity 换成官方品牌 logo。
- 设置里的按钮统一加高加宽；服务商列表的状态标记对齐成一列。
- 用量统计卡片改为浅色。
- 在线状态条使用与额度进度条相同的绿色，在线率数字放在状态条前面。
- 桌面卡片每个服务商按时间段显示，最多两条进度条。

#### 修复

- 悬浮卡片里的按钮能高亮但点击没有反应。
- 停靠条展开时出现空白条、收起时整体往上跳。
- 在线状态条颜色和在线率计算错误，正常的日子显示为红色。
- 切换停靠边缘后，停靠条仍停在原来那一侧。
- 新版 Grok CLI 的登录文件读不到，Grok 一直显示未配置。
- 本机残留旧版 Homebrew 安装记录时，被误判为 Homebrew 安装而无法应用内更新。

## 0.3.3 · 2026-09-12

#### 新增

- 通用二进制，同时原生支持 Apple 芯片和 Intel Mac。

#### 修复

- 后台刷新时每分钟弹出一次 Claude 钥匙串对话框。现在只有点击「授权钥匙串访问」时才会询问。

## 0.3.2 · 2026-08-31

#### 样式

- 用量颜色改为连续渐变：50% 以内为绿色，随后经金色、橙色过渡，到 90% 变为红色。
- 应用名 QuotaBar 使用 Sora 字体。
- 设置窗口整体为一块表面，顶部不再像多了一条标题栏。

#### 修复

- 设置窗口的关闭、最小化、缩放按钮消失。
- 菜单面板高度固定在首次打开时的空白状态，内容被截断。

## 0.3.1 · 2026-08-29

#### 新增

- 设置窗口改为侧边栏分区布局。
- 刘海岛收起时分布在刘海两侧，显示读数和重置倒计时。
- 停靠条单击圆环选中服务商，双击打开面板。
- Cursor 的指定模型用量单独显示一行。

#### 样式

- 设置侧边栏的选中项改为一条会移动的光带。
- 停靠条收起时显示一个把手，从底部按刻度填充当前用量。

#### 修复

- Cursor 实际用了 54% 却显示 100%。
- 停靠条收起时几乎看不见，也点不到。
- 停靠条、刘海岛、桌面卡片上的单色 logo 在浅色模式下变成黑色，看不见。
- 停靠条展开动画卡顿，分几段移动。

## 0.3.0 · 2026-08-29

#### 新增

- 停靠条：屏幕边缘的一列服务商圆环，悬停显示该服务商的额度卡片。
- 桌面小工具，三种尺寸，默认位于桌面层，可改为置顶。
- 花费明细：今日、昨日、近 30 天切换，并按来源显示环形图。OpenCode 作为第三个来源。
- 价格从公开价目表每日更新，内置价格表作为离线备用。
- 额度节奏：判断按当前速度会不会在重置前用完。
- 应用内更新：下载后校验签名与公证，确认无误才安装。

#### 修复

- 额度用满时趋势图被整块填满，看不出变化。
- 切换服务商时面板高度跳动。
- 未知模型被错误匹配到便宜的价格，花费明显偏低。

## 0.2.4 · 2026-08-27

#### 新增

- 菜单栏图标分两行显示短周期和长周期额度。
- 菜单栏图标跟随面板里选中的服务商，选择会被记住。

#### 修复

- 告警配色时额度耗尽和满额看起来一样。
- 告警颜色过浅，在菜单栏里发白。

## 0.2.3 · 2026-08-27

#### 新增

- 额度窗口按时长显示标记，如 5h、7d。
- 显示 Codex 的额度重置次数。

## 0.2.2 · 2026-08-27

#### 新增

- 自动读取本机 Cursor 和 OpenCode Go 的登录信息，无需手动粘贴。

#### 修复

- OpenCode Go 解析错误，一直没有数据。

## 0.2.1 · 2026-08-27

#### 修复

- 点击「设置」后窗口出现在其他窗口后面，看起来没有反应。

## 0.2.0 · 2026-08-27

#### 新增

- 五种新的菜单栏图标样式：格子、分段条、电池、仪表盘、刻度条。设置里直接显示图标供选择。
- 提供经过公证的 DMG 安装包。

#### 样式

- 强调色从荧光绿改为中性石墨色。
- 面板高度随内容伸展，不再截断。

#### 修复

- 重置时间文案出现「已重置后重置」。
- config.json 里一个无法识别的值会导致全部设置被重置。

## 0.1.0 · 2026-08-27

#### 新增

- 首个版本：在菜单栏显示 11 个 AI 编码服务的额度和重置时间。
- 中英双语界面，跟随系统语言。
- 手动填写的凭据只保存在系统钥匙串里。
- 每日花费柱状图，今日与近 30 天对比。
- 日志扫描速度提升约 7.7 倍。

#### 修复

- Claude Code 用量被重复计算，花费约为实际的两倍。
- Codex 缓存输入被重复计费。
- 按窗口长度命名额度，Pro 套餐不再被误标为 5 小时窗口。
